import{afterEach,expect,it,vi}from'vitest';import{cleanup,render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import TeacherClassesView from'./TeacherClassesView'
afterEach(()=>{cleanup();vi.restoreAllMocks()});it('navigates from assigned classes to assigned subjects',async()=>{const klass={id:'class-1',academicYearId:'year-1',name:'7А',gradeLevel:7,studentCount:12};vi.spyOn(globalThis,'fetch').mockResolvedValueOnce(new Response(JSON.stringify({items:[klass]}),{status:200})).mockResolvedValueOnce(new Response(JSON.stringify({items:[{id:'link-1',classId:'class-1',subject:{id:'subject-1',code:'MATH',name:'Математика'},responsibleTeacher:{id:'teacher-1',name:'Иван'+' '+'Петров',roles:['teacher']}}]}),{status:200}));render(<TeacherClassesView/>);await userEvent.click(await screen.findByRole('button',{name:'7А'}));expect(await screen.findByText('Математика')).toBeInTheDocument();expect(screen.queryByRole('button',{name:/Назначить/})).not.toBeInTheDocument()})
