import{afterEach,expect,it,vi}from'vitest'
import{cleanup,render,screen}from'@testing-library/react'
import userEvent from'@testing-library/user-event'
import ParentJournalView from'./ParentJournalView'
afterEach(()=>{cleanup();vi.restoreAllMocks()})
it('navigates to a read-only subject journal',async()=>{const student={id:'student-1',name:'Анна'+' '+'Иванова',roles:['student']},subject={id:'link-1',classId:'class-1',subject:{id:'s-1',code:'math',name:'Математика'},responsibleTeacher:{id:'t-1',name:'Иван'+' '+'Петров',roles:['teacher']}},period={academicYear:{id:'year-1',name:'2026/2027',startsOn:'2026-09-01',endsOn:'2027-05-31',status:'active',quarters:[]},class:{id:'class-1',academicYearId:'year-1',name:'7А',gradeLevel:7,studentCount:1},subjects:[subject]},lesson={id:'l-1',classSubjectId:'link-1',lessonDate:'2026-09-09',position:1,topic:'Дроби',homework:'№ 5',materials:[],absences:[{id:'a-1',lessonId:'l-1',studentId:'student-1'}],gradeItems:[{id:'g-1',lessonId:'l-1',title:'Работа',kind:'classwork',gradingScale:'five_point',scores:[{id:'score-1',gradeItemId:'g-1',studentId:'student-1',numericValue:5,teacherComment:'Отлично'}]}]};vi.spyOn(globalThis,'fetch').mockResolvedValueOnce(new Response(JSON.stringify({items:[student]}),{status:200})).mockResolvedValueOnce(new Response(JSON.stringify({items:[period]}),{status:200})).mockResolvedValueOnce(new Response(JSON.stringify({student,academicYear:period.academicYear,class:period.class,subjects:[{classSubject:subject,lessons:[{lesson,scores:[]}]}]}),{status:200}));render(<ParentJournalView/>);await userEvent.click(await screen.findByRole('button',{name:'Анна Иванова'}));await userEvent.click(await screen.findByRole('button',{name:'7А'}));await userEvent.click(await screen.findByRole('button',{name:'Математика'}));expect(await screen.findByText('Отлично')).toBeInTheDocument();expect(screen.getByText('Пропуск')).toBeInTheDocument();expect(screen.queryByRole('button',{name:'Сохранить'})).not.toBeInTheDocument()})
it('shows empty children state',async()=>{vi.spyOn(globalThis,'fetch').mockResolvedValueOnce(new Response(JSON.stringify({items:[]}),{status:200}));render(<ParentJournalView/>);expect(await screen.findByText('Нет доступных детей')).toBeInTheDocument()})
